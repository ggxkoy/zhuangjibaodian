# 立绘素材

把老板娘立绘放在本目录，命名 **`boss.jpg`**（含背景整图用 JPG 控制体积）：

- 竖构图（建议 1080×1920 以上），**背景合入图内**（整张图会 aspectFill 铺满舞台）
- 人物主体居中偏上，底部约 30% 留给对话框覆盖
- 缺图时页面自动降级为渐变场景 + emoji 占位，功能不受影响

想做表情差分（说话/开心/为难），可扩展为 boss-happy.png 等，
在 `pages/chat/chat.js` 里按剧情节点切换 `src` 即可。
